{
  "name": "ukulele-fingering-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node ./node_modules/@playwright/test/cli.js test tests/app.spec.js --browser=chromium --reporter=line",
    "dev": "npx --yes serve . --listen 5173"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
